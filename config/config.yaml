server:
  listen: "0.0.0.0:8000"
  auth:
    enabled: false
    api_key: "sk-xxx"
  admin_auth:
    enabled: false
    api_key: "sk-admin-xxx"

response:
  reasoning_mode: "field" # none | prefix | field | both
  include_usage: true
  schema_strict: true

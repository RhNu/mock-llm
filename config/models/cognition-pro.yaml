schema: 2
id: "cognition-pro"
extends: ["base-static"]
meta:
  owned_by: "llm-lab"
  created: 1738675200
kind: "static"
static:
  rules:
    - when:
        any:
          - contains: "secret"
          - regex: "/password|token/i"
      replies:
        - content: "抱歉，我无法提供该类敏感信息。"
          reasoning: "命中安全规则。"
    - when:
        any:
          - contains: "time"
          - contains: "date"
      replies:
        - content: "当前时间是 {{now}}。"
    - default: true
      replies:
        - content: "我是 cognition-pro，一个更高能力的 mock 模型。"

schema: 2
id: "llm-pro"
extends: ["base-static"]
meta:
  owned_by: "llm-lab"
  created: 1738675200
kind: "static"
static:
  rules:
    - when:
        any:
          - contains: "secret"
          - regex: "/password|token/i"
      replies:
        - content: "Sorry, I cannot help with that."
          reasoning: "Match-based safe reply."
    - when:
        any:
          - contains: "time"
          - contains: "date"
      replies:
        - content: "Current time is {{now}}."
    - default: true
      replies:
        - content: "I am llm-pro, a higher capability mock model."
